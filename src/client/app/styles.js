/* @flow */

import styled from 'styled-components';

import { size } from '@client/styles/constants';

export const Wrapper = styled.div``;

export const Main = styled.main`
  width: 100%;
  max-width: ${size.inner}px;
  margin: 0 auto;
  padding: 60px 0;
`;
