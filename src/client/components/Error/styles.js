/* @flow */

import styled from 'styled-components';

console.log(require('@client/styles/constants'));

export const Wrap = styled.p`
  display: inline-block;
  background-color: '';
`;

export default { Wrap };
